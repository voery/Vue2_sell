// 创建一个border-1px的函数   用来设置1px的边框的
border-1px($color)
  position: relative
  &:after
     display: block
     position: absolute
     left: 0
     bottom: 0
     width: 100%
     border-top: 1px solid $color
     content: ' '
// 创建一个border-none的函数   用来取消边框的
border-none()
  &:after
    display : none
// 创建一个bg-image的函数   用来设置不同的背景图片
bg-image($url)
  background-image: url($url + "@2x.png")
  @media (-webkit-min-device-pixel-ratio: 3),(min-device-pixel-ratio: 3)
    background-image: url($url + "@3x.png")
